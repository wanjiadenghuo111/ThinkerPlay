# Shared

Shared code belongs here when at least two experiments need it.

## Suggested Folders

- `components/`: reusable UI controls and panels.
- `hooks/`: reusable animation or lifecycle helpers.
- `utils/`: math, color, randomization, formatting, and data helpers.

Keep shared code small. A helper should move here only after duplication appears in real experiments.
